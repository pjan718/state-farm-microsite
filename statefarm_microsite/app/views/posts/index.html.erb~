<h1>Charity Resource Page</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Asset</th>
    <td>Image</td>
    <td>Video</td>
    <th>Visible</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<%= link_to 'Headers', headings_path %>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.name %></td>
    <td><%= post.description %></td>
    <td><%= if post.asset? then link_to('Download', post.asset.url) end %></td>
    <td><%= if post.photo? then link_to('Download', post.photo.url) end %></td>
    <td><%= if post.video? then link_to('Download', post.video.url) end %></td>
    <td><%= if admin_signed_in? 
	      post.visible 
	    end
	%>
    </td>
    <td><%= link_to 'Show', post %></td>
    <td><%= if admin_signed_in? 
		link_to 'Edit', edit_post_path(post) 
	    end %>
    </td>
    <td><%= if admin_signed_in? 
		link_to 'Delete', post, :confirm => 'Are you sure?', :method => :delete 
	    end
	%>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= if admin_signed_in? 
	link_to 'New Post', new_post_path 
    end
%>
